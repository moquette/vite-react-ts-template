# ============================================================================
# ENVIRONMENT CONFIGURATION EXAMPLE
# ============================================================================
# Copy this file to .env.local and customize the values for your environment.
#
# IMPORTANT:
# - All variables must be prefixed with VITE_ to be available in the client
# - Never commit .env.local files containing sensitive data
# - Use .env.local for local development overrides
# - Use deployment-specific environment variables for production
# ============================================================================

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# The display name of your application
# Used in: Page titles, headers, branding
# Default: "Vite React TS Template"
VITE_APP_NAME="My Enterprise App"

# Application version (typically matches package.json)
# Used in: About pages, footers, debugging
# Default: "0.0.0"
VITE_APP_VERSION="1.0.0"

# Environment identifier for conditional behavior
# Options: development | staging | production
# Used in: Feature flags, debugging, error handling
# Default: "development"
VITE_APP_ENV=development

# ============================================================================
# API CONFIGURATION
# ============================================================================

# Base URL for your backend API
# Examples:
#   Development: http://localhost:3001/api
#   Staging: https://api-staging.yourapp.com
#   Production: https://api.yourapp.com
# Default: "http://localhost:3000"
VITE_API_URL=http://localhost:3001/api

# API request timeout in milliseconds
# Recommended: 30000 (30 seconds) for most applications
# Default: 30000
VITE_API_TIMEOUT=30000

# ============================================================================
# FEATURE FLAGS
# ============================================================================
# Control which features are enabled in your application
# Use "true" to enable, "false" or omit to disable

# Enable Google Analytics or similar tracking
# When enabled, requires VITE_GOOGLE_ANALYTICS_ID
# Default: false
VITE_ENABLE_ANALYTICS=false

# Enable error tracking (Sentry, LogRocket, etc.)
# When enabled, requires VITE_SENTRY_DSN
# Default: false
VITE_ENABLE_ERROR_TRACKING=false

# ============================================================================
# THIRD-PARTY SERVICES
# ============================================================================
# These are optional and only needed if you're integrating external services
# Leave commented out if not using

# Stripe Public Key (for payment processing)
# Get from: https://dashboard.stripe.com/apikeys
# VITE_STRIPE_PUBLIC_KEY=pk_test_51234567890abcdef...

# Google Analytics Measurement ID
# Get from: https://analytics.google.com/
# Format: G-XXXXXXXXXX
# VITE_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Sentry DSN (for error tracking)
# Get from: https://sentry.io/settings/projects/
# VITE_SENTRY_DSN=https://1234567890abcdef@o123456.ingest.sentry.io/123456

# ============================================================================
# DEVELOPMENT NOTES
# ============================================================================
#
# Environment File Priority (Vite):
# 1. .env.[mode].local (highest priority)
# 2. .env.local
# 3. .env.[mode]
# 4. .env (lowest priority)
#
# Common Usage:
# - .env.local: Personal development overrides (never commit)
# - .env.development: Shared development defaults
# - .env.staging: Staging environment config
# - .env.production: Production environment config
#
# Accessing Variables:
# - In TypeScript: import.meta.env.VITE_API_URL
# - Type-safe access: Use src/config/env.ts
#
# Security Notes:
# - NEVER put sensitive data in environment variables
# - Use secrets management for API keys, tokens, etc.
# - All VITE_ variables are bundled into the client-side code
# - Consider them public information
#
# ============================================================================